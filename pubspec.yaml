name: exam
description: A new Flutter application to apply for a job.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  hive: ^2.0.4
  hive_flutter: ^1.0.0
  http: ^0.13.1
  provider: ^5.0.0
  adaptive_theme: ^2.2.0
  flash: ^1.5.1
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
